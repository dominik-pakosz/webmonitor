{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid">
        <nav class="navbar navbar-default webmonitor-navbar">
            <div class="container-fluid">
                <div class="navbar-header">
                    <a class="navbar-brand" href="#">Rodzaje zadań</a>
                </div>
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('list') }}">Lista</a></li>
                    <li class="active"><a href="{{ path('logs') }}">Logi</a></li>
                    <li><a href="{{ path('stats') }}">Statystyki</a></li>
                    <li>
                </ul>
            </div>
        </nav>
        <div class="row">
            <div class="col-md-10 col-md-push-1">
                {{ form_start(form, { 'attr': {'class': 'navbar-form', 'style': 'width: 200px;'} }) }}
                    {{ form_widget(form) }}
                    <button type="submit" class="btn btn-success">Stwórz</button>
                {{ form_end(form) }}
            </div>
        </div>
        <div class="row">
            <div class="col-md-2 col-md-push-1">
                <div style="margin-top:40px; margin-left: 15px;" class="btn-group" role="group" aria-label="...">
                    <a href="{{ path('type_index') }}"><button type="button" class="btn btn-default">Wróć</button></a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
